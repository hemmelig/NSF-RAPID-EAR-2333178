# data-summaries


Synchronise your environment to the work unit requirements:

```bash
uv sync
```

Run as:

```bash
uv run build-summary.py --station DYFM --archive ../../data/internal/archive
uv run build-summary.py --station KAFM --archive ../../data/internal/archive
uv run build-summary.py --station MOFM --archive ../../data/internal/archive
uv run build-summary.py --station OLFM --archive ../../data/internal/archive
uv run build-summary.py --station RIFM --archive ../../data/internal/archive
uv run build-summary.py --station STFM --archive ../../data/internal/archive
```
